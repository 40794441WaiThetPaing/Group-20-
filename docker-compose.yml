version: '3'
services:
  app:
    build: .
    depends_on:
      - db

  db:
    build: database/.
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: world
    ports:
      - "33060:3306"
